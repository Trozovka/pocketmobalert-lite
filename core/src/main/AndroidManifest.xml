<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--
      No general INTERNET permission here: the beacon/watch/alarm loop has zero network
      dependency (non-negotiable #1). Location/notifications/battery-exemption permissions come
      from the :reliability module dependency's own manifest. BLE and foreground-service-type
      permissions below are specific to this app and not part of that shared module, since no
      prior portfolio app used BLE peripheral/central mode.
    -->
    <!-- Legacy Bluetooth permissions, required for any Bluetooth API call at all on Android <=11
         (API <=30): normal protection level, auto-granted at install, no runtime prompt.
         Superseded by the three runtime permissions below on API 31+, hence maxSdkVersion. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Alarm behavior (M4): normal protection level, auto-granted at install. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application>
        <service
            android:name="com.trozovka.pocketmobalert.core.beacon.MobAlertBeaconService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <service
            android:name="com.trozovka.pocketmobalert.core.watch.MobAlertWatchService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location" />
    </application>
</manifest>
